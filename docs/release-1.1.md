# Release 1.1

## Major Changes
- Binance/Bithumb runtime credential management split (save/clear independently).
- Binance forced to mainnet (testnet disabled).
- Live order allow/deny toggle added in UI and backed by runtime policy API.
- API base visibility added in settings UI.
- System review/ops notes recorded.

## Ops Notes
- AWS backend updated from old repo to fresh clone.
- Runtime order policy persisted under `.runtime/execution-order-policy.json`.
